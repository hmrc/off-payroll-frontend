/*
 * Copyright 2021 HM Revenue & Customs
 *
 */

package assets.messages

object ExtendContractMessages extends BaseMessages {

  val error = "Select yes if the current contract allows for it to be extended"

  object Worker {
    val title = "Does the current contract allow for it to be extended?"
    val heading = "Does the current contract allow for it to be extended?"
    val subheading = "Worker’s contracts"
  }

  object Hirer {
    val title = "Does the current contract allow for it to be extended?"
    val heading = "Does the current contract allow for it to be extended?"
    val subheading = "Worker’s contracts"
  }

}
